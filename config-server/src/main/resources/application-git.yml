spring:
  config:
    activate:
      on-profile: git
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_REPOSITORY_GIT_URI}
          default-label: ${CONFIG_REPOSITORY_GIT_DEFAULT_LABEL:main}
          clone-on-start: true
